export interface User {
    name: string;
    password: string;
    rollNumber: string;
    email: string;
}
export interface userDetails {
    name: string;
    email: string;
    rollNumber: string;
}
